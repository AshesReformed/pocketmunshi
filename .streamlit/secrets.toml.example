# Copy this file to .streamlit/secrets.toml for local dev (that file is
# gitignored -- never commit real secrets). On Streamlit Community Cloud,
# paste these same keys/values into your app's Settings -> Secrets instead.

shop_username = "shopkeeper"
shop_password = "changeme123"

# Get a free key at https://console.groq.com
GROQ_API_KEY = "your-groq-api-key-here"
